name: wysiwyg_editor
description: A Detailed Rich Text Editor for Flutter
version: 2.2.0

environment:
  sdk: ">=3.1.0 <4.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

  provider: ^6.0.3
  random_string: ^2.3.1
  wysiwyg_flutter_quill: ^2.0.0
  wysiwyg_flutter_quill_extensions: ^2.0.0
  flutter_svg:

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
